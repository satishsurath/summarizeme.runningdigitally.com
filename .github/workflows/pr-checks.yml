# PR Quality Checks — runs on every PR and push to main
name: PR Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff (lint + format check)
        run: ruff check . && ruff format --check .

  tests:
    name: Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: summarizeme
          POSTGRES_PASSWORD: summarizeme_pass
          POSTGRES_DB: summarizeme_test
        ports:
          - 5432
        options: >-
          --health-cmd="pg_isready -U summarizeme -d summarizeme_test"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run unit tests
        env:
          DATABASE_URL: postgresql://summarizeme:summarizeme_pass@localhost:${{ job.services.postgres.ports[5432] }}/summarizeme_test
          REMOTE_OLLAMA_HOST: localhost
        run: pytest tests/unit/ -v --cov=app --cov=summarizer_v2 --cov=youtube_utils --cov=db --cov-report=xml

      - name: Run integration tests
        env:
          DATABASE_URL: postgresql://summarizeme:summarizeme_pass@localhost:${{ job.services.postgres.ports[5432] }}/summarizeme_test
          REMOTE_OLLAMA_HOST: localhost
          DEV_AUTH_ENABLED: "true"
        run: pytest tests/integration/ -v --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          files: ./coverage.xml
          fail_ci_if_error: false

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Dockerfile
        run: docker build -t summarizeme:test .

      - name: Check container can start
        run: docker run --rm -e DATABASE_URL=postgres://ignored -e REMOTE_OLLAMA_HOST=ignored -e DEV_AUTH_ENABLED=true summarizeme:test python -c "import app; print('OK')"
        continue-on-error: true
