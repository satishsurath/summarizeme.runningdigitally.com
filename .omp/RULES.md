Never commit, push, create or merge a pull request, deploy, mutate remote services,
or run destructive commands unless the user explicitly requests the exact action.

Never treat a skipped test, a mocked external service, a non-blocking CI job, a
README statement, recalled memory, or a graph edge as proof of runtime behavior.

Before editing, confirm the current Git branch, HEAD, and dirty state. Preserve
unrelated user changes.
