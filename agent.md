# Legacy agent notes

This file is retained only for compatibility with older tooling. The canonical
repository instructions are [`AGENTS.md`](AGENTS.md) and the OMP guidance in
[`.omp/`](.omp/). In particular, never commit, push, create a pull request,
merge, deploy, or change remote infrastructure unless the user explicitly asks.
